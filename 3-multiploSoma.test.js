var { multiploSoma } = require('./3-multiploSoma')

describe('Funções matemáticas', () => {
    
    it('Validar a soma dos multiplos de dois valores', () => {
        expect((multiploSoma))
    });
});